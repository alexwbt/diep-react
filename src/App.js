import React from 'react';
import Canvas from './Canvas';

const App = () => {
    return (
        <React.StrictMode>
            <Canvas />
        </React.StrictMode>
    );
};

export default App;
